{
  "name": "qr-scanner-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "rimraf dist && vite build",
    "preview": "vite preview --port 5173",
    "zip": "cross-env-shell \"cd dist && zip -r ../qr-scanner-extension.zip .\""
  },
  "dependencies": {
    "@zxing/browser": "^0.1.1",
    "@zxing/library": "^0.21.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "cross-env": "^7.0.3",
    "rimraf": "^5.0.5",
    "vite": "^5.4.0"
  }
} 